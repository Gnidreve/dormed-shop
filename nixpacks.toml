[variables]
NIXPACKS_PHP_VERSION = "8.3"

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction"
]

[phases.build]
cmds = [
    "npm ci",
    "npm run build"
]
